[project]
name = "centaur-household-overlay"
version = "0.1.0"
description = "Saia-Nitroy household overlay for Centaur"
requires-python = ">=3.11"
license = "Apache-2.0 OR MIT"
dependencies = []

[dependency-groups]
dev = ["pytest>=8.0.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
