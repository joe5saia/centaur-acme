[project]
name = "household-context"
version = "0.1.0"
description = "Saia-Nitroy household context helper"
requires-python = ">=3.11"
dependencies = ["click>=8.0.0"]

[project.scripts]
household-context = "cli:main"
